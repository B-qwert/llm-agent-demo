# 协作平台配置状态

检查日期：2026 年 9 月 9 日。

| 项目 | 状态 | 证据或下一步 |
| --- | --- | --- |
| 远程仓库 | 已完成 | 私有仓库 `B-qwert/llm-agent-demo`，默认分支为 `main` |
| 本地协作规范 | 已完成 | CONTRIBUTING、PR 模板、Git 忽略规则和 GitHub Actions 工作流均已提交 |
| CI | 已完成 | `offline` 与 `databases` 两个检查均已在 GitHub Actions 成功运行 |
| main 分支保护 | 受平台限制 | GitHub API 返回 403：私有仓库需要 GitHub Pro，或将仓库改为公开后才可启用分支保护或 rulesets |
| 三人协作成员 | 待补充 | 需要夏高婷、周笑彤的 GitHub 用户名，才能发送仓库协作者邀请 |

分支保护启用后，应要求 `offline`、`databases` 两项检查通过，至少一位非作者审批，审批在新提交后失效，禁止 force push 和分支删除，并要求解决 PR 对话。

在平台限制解除前，团队按 CONTRIBUTING 人工执行同一规则：不直接推送 `main`，所有变更走短期分支与 Pull Request，至少一位非作者审阅后再合并。
