# 本机执行状态

检查日期：2026 年 9 月 9 日。工作目录：`D:\Codex\codex_test\project`。

| 项目 | 实际结果 |
| --- | --- |
| Git | 已初始化 main 分支；.env 和 JSON 验收报告已验证被忽略；工程纳入首次提交 |
| 工程文件 | Compose、LLM 客户端、数据库验收、操作脚本、协作规范和 CI 已生成 |
| 离线验证 | 9 项单元测试通过；Python 与 PowerShell 语法检查通过；Docker Compose 配置校验通过 |
| WSL | 2.7.13.0 已安装，内核 6.18.33.2-2，默认版本为 2 |
| 虚拟机平台 | DISM 启用返回 3010，需要重启 |
| 固件虚拟化 | 当前系统报告 False；重启后复查，若仍关闭需到 BIOS 启用 |
| Docker Desktop | 官方安装包签名验证通过，安装返回 0；Docker CLI 29.7.2；引擎等待系统重启和虚拟化生效 |
| Neo4j / Milvus | 已提供部署及真实验收脚本；虚拟化未生效，尚未启动验证 |
| LLM 实际调用 | 未执行网络请求；检查报告为 FAIL，原因是未配置 LLM_BASE_URL |
| 远程协作 | 未提供远程地址，尚未推送、添加成员或启用分支保护 |

## 重启后继续

1. 保存其他工作后重启电脑。本次没有自动重启。
2. 在 PowerShell 执行 `wsl --status`。若仍报告虚拟化不可用，进入 BIOS/UEFI 启用 Intel Virtualization Technology，再启动 Windows。
3. 打开 Docker Desktop，完成首次启动提示，确认采用 WSL 2 与 Linux containers。
4. 打开新的 PowerShell，切换到本工程目录，运行 `powershell -File scripts/dev.ps1 up`。它会启动数据库并执行真实读写与检索验收。
5. 在本地 .env 填入 LLM 基础地址、模型名称与密钥，然后执行 `powershell -File scripts/dev.ps1 check`。只有三个服务全部 PASS 才满足整体连通性验收。
6. 提供远程仓库地址，按 CONTRIBUTING 配置三人成员与分支保护，再更新原始分工表中的完成状态。

若 WSL 已正常运行而 Docker 仍不可用，可先执行 `docker version` 区分 CLI 安装与引擎启动问题。不要把配置文件存在或单元测试通过当作数据库部署完成。
